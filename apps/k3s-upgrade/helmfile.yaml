environments:
  default:
    secrets:
      - ../globals.sops.yaml
---
releases:
  - name: k3s-upgrade-plans
    chart: ../../charts/k3s-upgrade-plans
    values:
      - k3s:
          version: v1.26.11+k3s2 # renovate: k3s
