environments:
  default:
    secrets:
      - ../globals.sops.yaml
      - default.sops.yaml
---
repositories:
  - name: helm-openldap
    url: https://jp-gouin.github.io/helm-openldap/

releases:
  - name: openldap
    chart: helm-openldap/openldap-stack-ha
    version: 4.1.1
    values:
      - global:
          ldapDomain: "{{ .Values.ldap.domain }}"
          adminPassword: "{{ .Values.ldap.admin.password }}"
          configPassword: "{{ .Values.configPassword }}"
      - phpldapadmin:
          ingress:
            annotations:
              traefik.ingress.kubernetes.io/router.entrypoints: websecure
            hosts:
              - ldap.{{ .Values.domain }}
      - ltb-passwd:
          enabled: false
      - customTLS:
          image:
            repository: openquantumsafe/openssl3 # replace alpine/openssl, they have arm64 images
      - persistence:
          enabled: true
          storageClass: longhorn-single
          size: 1Gi
