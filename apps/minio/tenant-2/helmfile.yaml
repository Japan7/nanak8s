environments:
  default:
    values:
      - tenant: 2
      - node: camp
      - jsonPatches:
          - target:
              group: minio.min.io
              version: v2
              kind: Tenant
              name: minio-tenant-2
            patch:
              - op: add
                path: /spec/pools/0/volumeClaimTemplate/metadata/labels
                value:
                  recurring-job.longhorn.io/source: enabled
                  recurring-job-group.longhorn.io/default: enabled
                  recurring-job.longhorn.io/longhorn-backup: enabled
---
bases:
  - ../tenant.helmfile.yaml
