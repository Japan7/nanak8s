environments:
  default:
    secrets:
      - ../globals.sops.yaml
      - default.sops.yaml
---
releases:
  - name: syncplay
    chart: ../../charts/syncplay
    values:
      - image:
          tag: master@sha256:9e7f3ac5f02b5431809c22d1ff03b5f42f7fd9a8a915206ad82954b953974381 # renovate: repository=ghcr.io/japan7/syncplay
      - syncplay:
          password: "{{ .Values.password }}"
          salt: "{{ .Values.salt }}"
      - ingress:
          host: syncplay.{{ .Values.domain }}
