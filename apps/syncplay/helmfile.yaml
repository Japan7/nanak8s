environments:
  default:
    secrets:
      - ../globals.sops.yaml
      - default.sops.yaml
---
releases:
  - name: syncplay
    chart: ../../charts/syncplay
    values:
      - image:
          tag: master
      - syncplay:
          password: "{{ .Values.password }}"
          salt: "{{ .Values.salt }}"
      - ingress:
          host: syncplay.{{ .Values.domain }}
