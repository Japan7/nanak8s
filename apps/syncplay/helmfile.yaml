environments:
  default:
    secrets:
      - ../shared.sops.yaml
      - default.sops.yaml
---
releases:
  - name: syncplay
    chart: ../../charts/syncplay
    namespace: syncplay
    values:
      - syncplay:
          password: "{{ .Values.password }}"
          sale: "{{ .Values.salt }}"
      - ingress:
          host: syncplay.{{ .Values.domain }}
