environments:
  default:
    secrets:
      - ../../globals.sops.yaml
---
repositories:
  - name: elastic
    url: https://helm.elastic.co

releases:
  - name: elasticsearch
    chart: elastic/elasticsearch
