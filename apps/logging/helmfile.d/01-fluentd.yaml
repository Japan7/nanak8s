environments:
  default:
    secrets:
      - ../../globals.sops.yaml
---
repositories:
  - name: fluent
    url: https://fluent.github.io/helm-charts

releases:
  - name: fluentd
    chart: fluent/fluentd
    values:
      - podSecurityPolicy:
          enabled: false
