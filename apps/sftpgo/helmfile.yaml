environments:
  default:
    secrets:
      - ../globals.sops.yaml
      - default.sops.yaml
---
releases:
  - name: sftpgo
    chart: ../../charts/sftpgo
    values:
      - updateStrategy:
          type: Recreate
      - system:
          config: |
            {{ .Values.config | nindent 12 }}
          persistence:
            labels:
              recurring-job.longhorn.io/source: enabled
              recurring-job-group.longhorn.io/default: enabled
              recurring-job.longhorn.io/longhorn-backup: enabled
      - data:
          persistence:
            labels:
              recurring-job.longhorn.io/source: enabled
              recurring-job-group.longhorn.io/default: enabled
              recurring-job.longhorn.io/longhorn-backup: enabled
      - ingress:
          host: sftp.{{ .Values.domain }}
          http:
            annotations:
              traefik.ingress.kubernetes.io/router.entrypoints: websecure
              cert-manager.io/cluster-issuer: letsencrypt
      - extraContainers:
          - name: hooks
            image: ghcr.io/japan7/sftpgo-oidc-hooks:main@sha256:b6a6ab2ced0eaf2f44941bb47b530182364fb07d8bb34e1fc605a942310753f7 # renovate: repository=ghcr.io/japan7/sftpgo-oidc-hooks
            ports:
              - containerPort: 8000
