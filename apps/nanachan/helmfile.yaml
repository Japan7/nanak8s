environments:
  prod:
    values:
      - tag: master
    secrets:
      - ../shared.sops.yaml
      - prod.sops.yaml
  staging:
    values:
      - tag: master
    secrets:
      - ../shared.sops.yaml
      - staging.sops.yaml
  dev:
    values:
      - tag: 8545fc43458ce804f084b5714b4520eede3d85b0
    secrets:
      - ../shared.sops.yaml
      - dev.sops.yaml
---
releases:
  - name: nanachan-{{ .Environment.Name }}
    chart: ../../charts/nanachan
    namespace: nanachan
    values:
      - image: git.{{ .Values.domain }}/japan7/nanachan
      - tag: "{{ .Values.tag }}"
      - localSettings: |
          {{ .Values.localSettings | nindent 10 }}
