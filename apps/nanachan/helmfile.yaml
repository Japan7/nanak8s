environments:
  nana:
    values:
      - tag: ffcd5353069880c60fe5f21080055c0dacb55a9e
    secrets:
      - ../shared.sops.yaml
      - nana.sops.yaml
  hachi:
    values:
      - tag: ffcd5353069880c60fe5f21080055c0dacb55a9e
    secrets:
      - ../shared.sops.yaml
      - hachi.sops.yaml
  snow:
    values:
      - tag: e4cb659e8c705e43f7b299e2d11c1267e5ec502a
    secrets:
      - ../shared.sops.yaml
      - snow.sops.yaml
---
repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami
releases:
  - name: nanachan-{{ .Environment.Name }}-redis
    chart: bitnami/redis
    namespace: nanachan
    values:
      - architecture: standalone
      - auth:
          enabled: false
      - master:
          persistence:
            size: 1Gi
  - name: nanachan-{{ .Environment.Name }}
    chart: ../../charts/nanachan
    namespace: nanachan
    values:
      - image: git.{{ .Values.domain }}/japan7/nanachan
      - tag: "{{ .Values.tag }}"
      - localSettings: |
          {{ .Values.localSettings | nindent 10 }}
