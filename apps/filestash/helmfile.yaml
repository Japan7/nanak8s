environments:
  default:
    secrets:
      - ../globals.sops.yaml
---
releases:
  - name: filestash
    chart: ../../charts/filestash
    values:
      - image:
          tag: subs
          pullPolicy: Always
      - ingress:
          host: filestash.{{ .Values.domain }}
