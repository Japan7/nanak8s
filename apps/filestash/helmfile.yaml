environments:
  default:
    secrets:
      - ../shared.sops.yaml
---
releases:
  - name: filestash
    chart: ../../charts/filestash
    namespace: filestash
    values:
      - ingress:
          host: filestash.{{ .Values.domain }}
