apiVersion: v1
kind: Secret
metadata:
  name: github-secret
type: Opaque
stringData:
  WOODPECKER_GITHUB_CLIENT: "{{ .Values.github.client }}"
  WOODPECKER_GITHUB_SECRET: "{{ .Values.github.secret }}"
