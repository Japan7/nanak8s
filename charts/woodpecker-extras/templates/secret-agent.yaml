apiVersion: v1
kind: Secret
metadata:
  name: agent-secret
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: extras
    app.kubernetes.io/part-of: woodpecker-server
type: Opaque
stringData:
  WOODPECKER_AGENT_SECRET: "{{ .Values.agentSecret }}"
