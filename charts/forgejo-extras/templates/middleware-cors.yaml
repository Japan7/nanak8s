apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: forgejo-cors
spec:
  headers:
    accessControlAllowOriginList:
      {{- range .Values.cors.allowedOrigins }}
      - {{ . }}
      {{- end }}
