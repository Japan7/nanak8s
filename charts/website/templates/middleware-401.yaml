apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: "{{ .Release.Name }}-401"
spec:
  errors:
    status:
      - "401"
    query: /errors/401
    service:
      name: "{{ .Release.Name }}"
      port: 8080
