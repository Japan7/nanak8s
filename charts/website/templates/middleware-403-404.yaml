apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: "{{ .Release.Name }}-403-404"
spec:
  errors:
    status:
      - "403-404"
    query: /errors/404
    service:
      name: "{{ .Release.Name }}"
      port: 8080
