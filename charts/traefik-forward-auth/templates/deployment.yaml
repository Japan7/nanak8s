apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    matchLabels:
      app: "{{ .Release.Name }}"
  template:
    metadata:
      labels:
        app: "{{ .Release.Name }}"
    spec:
      containers:
        - name: "{{ .Chart.Name }}"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: "{{ .Values.image.pullPolicy }}"
          env:
            - name: PROVIDER_URI
              value: https://{{ .Values.keycloak.host }}/auth/realms/{{ .Values.keycloak.realm }}
            - name: CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-client
                  key: id
            - name: CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-client
                  key: token
          ports:
            - containerPort: 4181
