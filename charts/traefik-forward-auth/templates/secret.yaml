apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}"
type: Opaque
stringData:
  secret: "{{ .Values.secret }}"
  clientId: "{{ .Values.keycloak.client.id }}"
  clientToken: "{{ .Values.keycloak.client.secret }}"
