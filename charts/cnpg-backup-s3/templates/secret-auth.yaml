apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.auth.accessKey }}
  password: {{ .Values.auth.secretKey }}
