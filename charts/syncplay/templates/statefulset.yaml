apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: "{{ .Release.Name }}"
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "common.labels.matchLabels" . | nindent 6 }}
  serviceName: "{{ .Release.Name }}-headless"
  template:
    metadata:
      labels:
        {{- include "common.labels.standard" . | nindent 8 }}
    spec:
      volumes:
        - name: certs
          emptyDir: {}
      containers:
        - name: "{{ .Chart.Name }}"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: "{{ .Values.image.pullPolicy }}"
          env:
            - name: SYNCPLAY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ .Release.Name }}-secret"
                  key: password
            - name: SYNCPLAY_SALT
              valueFrom:
                secretKeyRef:
                  name: "{{ .Release.Name }}-secret"
                  key: salt
          args:
            - --tls
            - /certs
            - --rooms-db-file
            - /rooms/rooms
            - --permanent-rooms-file
            - /rooms/permanent_rooms
          volumeMounts:
            - name: certs
              mountPath: /certs
            - name: rooms
              mountPath: /rooms
          ports:
            - containerPort: 8999
      initContainers:
        - name: init-certs
          image: alpine
          command:
            - sh
            - -xc
            - |
              cd /certs && \
              rm -f *.pem && \
              wget {{ .Values.certsDumperService }}/{{ .Values.ingress.host }}/fullchain.pem && \
              wget {{ .Values.certsDumperService }}/{{ .Values.ingress.host }}/privkey.pem
          volumeMounts:
            - name: certs
              mountPath: /certs
  volumeClaimTemplates:
    - metadata:
        name: rooms
      spec:
        accessModes:
          - "ReadWriteOnce"
        resources:
          requests:
            storage: 64Mi
