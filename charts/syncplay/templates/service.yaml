apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    app: "{{ .Release.Name }}"
  ports:
    - name: syncplay
      port: 8999
    - name: http-stub
      port: 80
