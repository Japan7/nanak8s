apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-headless"
spec:
  selector:
    app: "{{ .Release.Name }}"
  clusterIP: None
  ports:
    - port: 8999
      name: syncplay
    - port: 80
      name: http
