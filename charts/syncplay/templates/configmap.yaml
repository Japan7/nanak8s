kind: ConfigMap
apiVersion: v1
metadata:
  name: "{{ .Release.Name }}-rooms"
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  permanent_rooms: |
    {{- range .Values.syncplay.rooms }}
    {{ . }}
    {{- end }}
