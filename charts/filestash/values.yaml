image:
  repository: machines/filestash
  tag: latest@sha256:02b6436dc4d08f1b79d8a803b24958f253e3009a4b17ceed0f2994552a168830
  pullPolicy: ""

replicaCount: 1

updateStrategy:
  type: RollingUpdate

nodeSelector:
  kubernetes.io/arch: amd64 # no arm64 image available

persistence:
  accessMode: ReadWriteOnce
  size: 1Gi
  mountPath: /app/data/state

cache:
  mountPath: /app/data/cache

onlyoffice:
  enabled: false
  image:
    repository: onlyoffice/documentserver
    tag: 7.4.1.1
    pullPolicy: ""

ingress:
  host: ""
  entryPoints:
    - websecure
