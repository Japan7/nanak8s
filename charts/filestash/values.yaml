image:
  repository: machines/filestash
  tag: latest@sha256:6e3fce8b0bd76216d4320a5fc81a8e26588fc59fee7d68aa6e4632d01adfd7a8
  pullPolicy: ""

replicaCount: 1

updateStrategy:
  type: RollingUpdate

nodeSelector:
  kubernetes.io/arch: amd64 # no arm64 image available

persistence:
  accessMode: ReadWriteOnce
  size: 1Gi
  mountPath: /app/data/state

cache:
  mountPath: /app/data/cache

onlyoffice:
  enabled: false
  image:
    repository: onlyoffice/documentserver
    tag: 7.4.1.1
    pullPolicy: ""

ingress:
  host: ""
  entryPoints:
    - websecure
