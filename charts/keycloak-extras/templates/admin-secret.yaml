apiVersion: v1
kind: Secret
metadata:
  name: admin-secret
  namespace: keycloak
type: kubernetes.io/basic-auth
stringData:
  username: "{{ .Values.admin.username }}"
  password: "{{ .Values.admin.password }}"
