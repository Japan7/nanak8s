apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
stringData:
  apiKey: "{{ .Values.filestash.apiKey }}"
  keycloakClientSecret: "{{ .Values.keycloak.clientSecret }}"
