apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: website-runner
spec:
  storageClassName: "{{ .Values.storageClassName }}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
